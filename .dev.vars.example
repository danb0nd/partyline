# Copy to .dev.vars (never commit .dev.vars).
# wrangler secret put SESSION_SECRET   # production

SESSION_SECRET=dev-only-change-me-not-for-prod

# Humans sign up / log in with email + password in the UI.
# Optional instant login API (not shown in the UI). Never set true in production.
# DEV_AUTH=true
